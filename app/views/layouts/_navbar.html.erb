<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation-bar" aria-expanded="false">
        <span class="sr-only"><%= t('navbar.toggle_nav') %></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to t('navbar.site_name'), root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navigation-bar">
      <ul class="nav navbar-nav">
        <li><%= link_to t('navbar.person_link'), person_index_path %></li>
        <li><%= link_to t('navbar.group_link'), group_index_path %></li>
        <li><%= link_to t('navbar.body_link'), body_index_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to t('navbar.about_link'), page_path('about-us') %></li>
      </ul>
    </div>
    <!-- /.navbar-collapse -->
  </div>
  <!-- /.container-fluid -->
</nav>
