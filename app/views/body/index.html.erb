<% title t('body.index.title') %>
<div class="container">
  <h2><%= t('body.index.title') %></h2>
  <% for body_type in @bodies %>
  <% unless body_type.bodies.count < 1 %>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title"><%= link_to body_type.name, body_type %></h3>
    </div>
    <div class="panel-body">
      <div class="table-responsive">
        <table class="table sortable-theme-bootstrap" data-sortable>
          <thead>
            <th><%= t('body.index.name') %></th>
            <th><%= t('body.index.number_of_members') %></th>
          </thead>
          <tbody>
            <% for body in body_type.bodies %>
            <tr>
              <td><%= link_to body.name, body %></td>
              <td><%= body.current_members.count %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
